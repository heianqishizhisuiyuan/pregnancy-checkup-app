---
description: 明确不做/不排期的功能，避免重复提议或实现
alwaysApply: true
---

# 明确不做（Out of Scope）

以下功能**不在本项目范围内**，除非用户明确要求重新纳入：

## 1. OSS 云存储

- 不做阿里云 OSS / 腾讯云 COS 等云存储迁移
- 不做存储层抽象（本地 ↔ 云）
- 附件继续走本地 `uploads/` + 现有 `fileAccess` 中间件
- 备份依赖 `scripts/backup.sh`（mongodump + uploads 打包）

## 2. 忘记密码 / 邮箱重置

- 不做忘记密码流程
- 不做邮件服务集成（SMTP、验证码邮件等）
- 账号安全仅保留：登录、注册、修改密码（需旧密码）、修改昵称

## 向用户建议时

- 优化附件存储时，优先：备份脚本、磁盘监控、上传限流——**不要**提议 OSS
- 优化登录体验时，优先：记住邮箱、JWT 滑动续期——**不要**提议忘记密码
