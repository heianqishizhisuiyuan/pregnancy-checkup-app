# 产检记录应用 - 后续迭代计划

> **用途：** 对照 `docs/design.md` 与当前代码差距梳理的可执行 backlog。每项可独立开发，建议按优先级从上到下逐个勾选完成。
>
> **执行方式：** 开始某一项前，先读相关 spec/plan；完成后将 `- [ ]` 改为 `- [x]`，并同步 `PROJECT_SUMMARY.md` / `README.md`（如有用户可见变化）。
>
> **最后更新：** 2026-07-03

---

## 当前基线（已完成，无需重复做）

以下能力已在 MVP / 第二阶段 / 2026-07-03 高价值迭代中落地：

- [x] 用户认证（注册、登录、JWT、owner/family 权限）
- [x] 产检记录 CRUD + 生理指标
- [x] 家庭信息与孕周/预产期计算
- [x] 图片上传与管理（分类、标签、权限）
- [x] 时间轴页面
- [x] **家人邀请码加入家庭**（注册加入、设置页复制/重新生成、成员列表）
- [x] **记录搜索与筛选**（关键词、医院、日期、孕周）
- [x] **数据趋势图表**（`/trends`，体重/血压/宫高/胎心）
- [x] **数据导出**（Excel 下载；PDF 通过打印预览另存为）
- [x] 生产部署脚本（`scripts/redeploy.ps1`、`scripts/deploy-app.sh`）
- [x] 基础单元测试（前后端工具函数、部分中间件）

---

## 优先级说明

| 级别 | 含义 | 建议 |
|------|------|------|
| P0 | 体验缺口小、改动集中、立刻有感 | 优先做 |
| P1 | 家庭共用 / 数据安全 / 长期可用 | 第二批 |
| P2 | 设计文档第三阶段、工程化增强 | 按需排期 |
| P3 | 锦上添花，个人项目可暂缓 | 有余力再做 |

---

## P0 · 小步体验优化（1–2 天可做完）

### 1. 新建记录自动填充孕周与默认日期

- [ ] **目标：** 减少手工输入，与家庭「末次月经」联动
- [ ] **范围：**
  - `RecordForm.vue` 新建模式：产检日期默认今天
  - 选择产检日期后，用 `calculateGestationalAge(lastPeriod, checkupDate)` 自动填孕周/天数
  - 用户仍可手动覆盖
- [ ] **依赖：** `familyStore` 或注册时写入的末次月经
- [ ] **验证：** 有末次月经时新建记录，孕周与手动计算一致；无末次月经时不报错、不自动填

### 2. 时间轴页导航与数据复用

- [ ] **目标：** 与时间轴/首页体验一致，避免重复请求
- [ ] **范围：**
  - `Timeline.vue` 增加返回首页导航栏（对齐 `Home.vue` / `Trends.vue`）
  - 优先复用 Pinia `recordStore`，与首页数据同步
- [ ] **验证：** 首页新增记录后，进入时间轴无需刷新即可看到（或一次 load 即可）

### 3. 大文件上传超时

- [ ] **目标：** 弱网 / 10MB 图片上传不因 10s 全局 timeout 失败
- [ ] **范围：**
  - `attachment.js` 或上传专用 axios 实例：`timeout: 60000`（或更长）
  - 不影响普通 API 的 10s 超时
- [ ] **验证：** 上传接近 10MB 的图片在本地/生产可成功

### 4. 文档与 PROJECT_SUMMARY 同步

- [ ] **目标：** 文档反映真实功能，避免误判进度
- [ ] **范围：**
  - 更新 `README.md`：图片上传、时间轴、邀请码、筛选、趋势、导出、部署说明
  - 更新 `PROJECT_SUMMARY.md`：将已完成功能从「未来优化」移到「已完成」；删除或修正「Token 自动续期」等未实现描述
  - 可选：在 `CLAUDE.md` API 快速参考中补充 family invite、records 查询参数
- [ ] **验证：** 新人仅读 README 即可了解当前能力

---

## P1 · 可靠性与家庭使用（3–7 天）

### 5. 个人资料与账号安全

- [ ] **目标：** 用户可改昵称、改密码；设计文档中的 `profile.avatar` 可后置
- [ ] **范围（建议分两步）：**
  - **5a** `PUT /api/auth/profile`（昵称）；前端「设置」或独立资料页
  - **5b** `PUT /api/auth/password`（旧密码 + 新密码）
  - **5c（可选）** 忘记密码 / 邮箱重置（需邮件服务，复杂度高，可单独排期）
- [ ] **验证：** owner/family 均可改自己的昵称；改密码后旧 token 失效或仍可接受（需在设计里二选一）

### 6. JWT 续期策略（修正「自动续期」表述）

- [ ] **目标：** 明确 7 天过期行为，减少使用中突然掉线
- [ ] **方案（二选一，实施前在 task 内写清）：**
  - **A：** 滑动续期 — 每次有效请求签发新 token（改动小）
  - **B：** refresh token — 双 token 机制（更安全，改动大）
- [ ] **范围：** 后端签发逻辑 + 前端 `request.js` / `authStore` 更新 token
- [ ] **验证：** 连续使用超过 7 天不必重新登录（若选 A）；或 refresh 流程 E2E 通过（若选 B）

### 7. 数据备份与恢复

- [ ] **目标：** 防止 MongoDB / `uploads/` 丢失（设计文档 14.3 已提醒）
- [ ] **范围：**
  - 新增 `scripts/backup.sh`：`mongodump` + `uploads/` 打包
  - 可选 cron 说明写入 `QUICK_START.md` 或部署文档
  - 可选 `scripts/restore.sh` 恢复指引
- [ ] **验证：** 在 staging/本地执行备份，能还原一条记录 + 一张图片

### 8. 健康检查增强

- [ ] **目标：** `/health` 能反映数据库可用性，便于监控与 redeploy 脚本判断
- [ ] **范围：** `app.js` 中检测 `mongoose.connection.readyState`，DB 不可用时返回 503
- [ ] **验证：** 停 MongoDB 后 `/health` 非 200；`redeploy.ps1` 健康检查能捕获失败

### 9. 列表分页

- [ ] **目标：** 记录增多后首页/时间轴/趋势仍流畅
- [ ] **范围：**
  - 后端 `GET /api/records?page=&limit=`（默认 limit 如 20）
  - 前端首页滚动加载或分页器；趋势页可仍拉全量或单独 `GET /api/records/stats`（实施时选型）
- [ ] **验证：** 100+ 条 mock 数据下首屏加载时间可接受

---

## P2 · 设计文档第三阶段

> 参考 `docs/design.md` §13.2

### 10. 产检提醒

- [ ] **目标：** 按预产期/自定义节奏提醒下次产检
- [ ] **范围：**
  - 家庭或记录级「下次产检日期」字段
  - 前端 Notification API 请求权限 + 本地提醒
  - 可选：后端 node-cron + 推送（需额外基础设施）
- [ ] **验证：** 到设定日期前 1 天浏览器通知触发（需用户授权）

### 11. PWA 离线支持

- [ ] **目标：** 可「添加到主屏幕」，弱网可读已缓存页面
- [ ] **范围：**
  - `public/manifest.json`、图标
  - Vite PWA 插件或手写 Service Worker（缓存静态资源 + 可选 API 策略）
  - 明确离线时只读，写操作需联网
- [ ] **验证：** Lighthouse PWA 基线；断网可打开已访问过的首页

### 12. 图片云存储（OSS）

- [ ] **目标：** 降低服务器磁盘风险，便于迁移
- [ ] **范围：**
  - 抽象存储层（本地 / 阿里云 OSS 等）
  - 迁移脚本：现有 `uploads/` → OSS，更新 DB path
  - `fileAccess` 中间件适配签名 URL 或代理
- [ ] **验证：** 新上传走 OSS；旧图片仍可访问

### 13. PDF 直接下载（非打印）

- [ ] **目标：** 一键下载 PDF 文件，无需打印对话框
- [ ] **范围：**
  - 方案：嵌入中文字体到 jspdf，或后端 pdfkit 生成
  - 替换或补充现有 `exportRecordsToPdf` 打印方案
- [ ] **验证：** 下载的 PDF 中文正常、表格完整

### 14. 导出增强

- [ ] **目标：** 导出含图片摘要或按筛选条件命名
- [ ] **范围：** Excel 增加附件数量/链接列；PDF 可选缩略图（工作量大，可拆分）
- [ ] **验证：** 导出文件与当前筛选条件一致（已实现筛选导出，本项为增强）

---

## P2 · 工程与质量

### 15. API 限流

- [ ] **目标：** 降低登录/注册/上传接口被暴力尝试风险
- [ ] **范围：** `express-rate-limit` 作用于 `/api/auth/login`、`register`、附件上传
- [ ] **验证：** 短时间大量请求返回 429

### 16. 后端 API 集成测试

- [ ] **目标：** 覆盖核心流程，防止回归
- [ ] **范围：**
  - 使用 mongodb-memory-server 或 test DB
  - 覆盖：注册、邀请码加入、records CRUD、筛选查询、family invite
- [ ] **验证：** `pnpm test` 含集成用例且 CI 可跑

### 17. E2E 测试（可选）

- [ ] **目标：** 注册 → 建记录 → 上传 → 导出 主路径
- [ ] **范围：** Playwright 或 Cypress，最少 1 条 happy path
- [ ] **验证：** 本地 `pnpm e2e` 通过

### 18. CI/CD（GitHub Actions）

- [ ] **目标：** push/PR 自动跑 test + frontend build
- [ ] **范围：** `.github/workflows/ci.yml`
- [ ] **验证：** PR 上 checks 绿

### 19. API 文档（Swagger）

- [ ] **目标：** 接口可浏览、可给家人/协作者查阅
- [ ] **范围：** swagger-jsdoc + swagger-ui-express，或 OpenAPI 静态 YAML
- [ ] **验证：** `/api-docs` 可打开且与实现一致

### 20. 错误日志与监控

- [ ] **目标：** 生产问题可追查
- [ ] **范围：** 结构化日志（winston/pino）；可选 Sentry；PM2 日志轮转说明
- [ ] **验证：** 故意 500 可在日志中定位 stack

---

## P3 · 体验锦上添花

### 21. 骨架屏加载

- [ ] **范围：** `Home.vue`、`RecordDetail.vue`、`Trends.vue` 首屏 skeleton 替代纯 spinner
- [ ] **验证：** 慢网下列表区域有占位结构

### 22. 移动端适配打磨

- [ ] **范围：** 导航栏换行、FAB 位置、筛选栏折叠、图表在小屏高度
- [ ] **验证：** iPhone/Android 浏览器手动走查清单通过

### 23. 图片懒加载与缩略图

- [ ] **范围：** `AttachmentGallery` lazy load；列表接口已返回 `attachmentCount`，详情页可请求缩略图或压缩图
- [ ] **验证：** 多图记录详情首屏请求数减少

### 24. 深色模式

- [ ] **范围：** CSS 变量 + 切换开关；Element Plus dark 变量
- [ ] **验证：** 切换后各主要页面可读

### 25. 多语言（i18n）

- [ ] **范围：** vue-i18n；先抽离首页/表单文案
- [ ] **验证：** 中英文切换至少 2 个页面完整

---

## 已知设计 vs 实现差异（跟踪用）

| 项 | 设计/文档描述 | 当前实现 | 建议任务 |
|----|---------------|----------|----------|
| Token 自动续期 | PROJECT_SUMMARY 声称有 | 7 天 JWT，401 跳登录 | → 任务 6 |
| PWA | 项目称 Web 应用（PWA） | 无 manifest/SW | → 任务 11 |
| 首页统计 | 3 项含产检次数 | 已实现 3 张卡 | 文档同步即可 |
| 列表附件 | 全量 attachments | 列表仅 `attachmentCount` | 详情页不变；任务 23 可选缩略图 |
| 家人加入 | 邀请码 | 已实现 | — |
| 数据导出 | PDF/Excel | Excel + 打印式 PDF | → 任务 13/14 |

---

## 建议执行顺序（个人项目）

若一次只做一个，推荐顺序：

1. **任务 4**（文档同步）— 成本低，立刻清晰  
2. **任务 1**（自动孕周）— 日常录入最常用  
3. **任务 3**（上传超时）— 避免生产踩坑  
4. **任务 2**（时间轴导航）  
5. **任务 8**（健康检查）  
6. **任务 7**（备份）  
7. **任务 5**（改密码/昵称）  
8. **任务 6**（JWT 策略）  
9. **任务 9**（分页）  
10. 再按需要选 P2/P3  

---

## 相关文档

- 系统设计：`docs/design.md`
- MVP 计划：`docs/superpowers/plans/2026-07-02-pregnancy-checkup-mvp.md`
- 第二阶段：`docs/superpowers/plans/2026-07-02-phase2-features-implementation.md`
- 项目现状：`PROJECT_SUMMARY.md`
- 快速启动：`QUICK_START.md`

---

**维护说明：** 每完成一项，在本文件勾选 checkbox，并在 `PROJECT_SUMMARY.md` 的「已完成 / 未来优化」中保持一致。
